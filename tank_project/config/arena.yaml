# Arena Configuration
# Generated by calibration wizard

display:
  # Display settings (MUST be identical for calibration and game)
  fullscreen: true # true = fullscreen without borders, false = windowed
  display_index: 1 # 0 = primary monitor (PC), 1 = secondary (projector)

projector:
  width: 1920 # MUST match projector native resolution
  height: 1080
  margin_px: 50 # Safe zone margin to avoid ArUco detection issues

arena:
  width_m: 2.85 # Measured during calibration
  height_m: 1.90

transform:
  # Homography matrix Camera → World (3x3)
  # Set during calibration
  H_C2W: null

  # Metric scale factor (meters per AV unit)
  scale_m_per_av: 1.15

grid:
  resolution_m: 0.02 # 2cm grid cells
  inflation_radius_m: 0.24 # Safety margin around obstacles

obstacles:
  # Static obstacle regions (from calibration)
  # Each obstacle: {type: 'rect'/'circle', params: ...}
  static: []
